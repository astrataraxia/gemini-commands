name = "review-pr"
description = "Review a GitHub Pull Request using AI(한국어)"

[prompt]
file = "review-pr.md"

[arguments]
id = { help = "Pull Request number to review", required = true }

[execution]
fetch_command = "gh pr diff {{id}} --color=never"

output_file = "reviews/review-{{id}}.md"